
// this code can be used in console of codeWars to see your rank in a group.
// check the index of people with similar points to see oyour rank.

let list = Array.from(document.querySelectorAll('tr'))
list = list.map(el => el = el.innerText)
console.log(list)



